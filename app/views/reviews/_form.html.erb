<div class="d-flex inline text-center mt-5 ms-5">
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :rating %>
    <%= f.input :content %>
    <%= f.button :submit, class: 'btn btn-primary' %>
  <% end %>
</div>
